import 'package:flutter/material.dart';

// ignore: non_constant_identifier_names
Widget Actions() {
  return IconButton(
    onPressed: () => {},
    icon: const Icon(
      Icons.money,
      color: Colors.white,
    ),
  );
}
